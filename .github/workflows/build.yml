name: Build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
      name: Checkout
      with:
        fetch-depth: 1
    - name: Install NPM dependencies
      run: npm install
    - name: Build web extension
      run: make build

